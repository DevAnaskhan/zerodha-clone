# MongoDB Atlas connection string. Keep the real value only in local .env and Vercel.
MONGO_URL=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority

# Comma-separated deployed web origins allowed to call this API.
FRONTEND_ORIGINS=https://your-dashboard.vercel.app
